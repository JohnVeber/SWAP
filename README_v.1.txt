Current version: 1.1.0

Short name version: v.1

Description:
1.0.1. 
A simple method to swap values of two elements of one type (this version works with integers only).

1.0.2. 
New feature: Implemented the ability to enter values, which need to be swapped.

1.1.0. 
The program is divided into several files (one additional header and another .c file with definition of called function). CMake used as build automatisation tool.
